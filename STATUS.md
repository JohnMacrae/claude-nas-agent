# Status — 18/07/2026

**State:** Running on OpenRouter (`google/gemini-2.5-flash`). Voice `/command` OK; tenant lookup from WO PDFs OK.
**Last activity:** dcp — OpenRouter agent + `/command` + `wo_lookup` committed.
**Blocker:** WO PDFs not auto-saved on intake; `GOOGLE_REFRESH_TOKEN` unset.
**Next:** Auto-save WO PDFs → `output/work_orders`; Calendar OAuth; Siri Shortcut.
